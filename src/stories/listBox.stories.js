import React from "react";

import ListBox from "../components/common(공통컴포넌트)/ListBox";

export default {
  title: "listBox",
  component: ListBox,
};

export const Template = (args) => <ListBox {...args} />;
